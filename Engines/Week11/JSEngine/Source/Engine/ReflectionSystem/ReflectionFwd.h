#pragma once

// ============================================================
//  ReflectionFwd.h
//  리플렉션 시스템 전체의 전방 선언 모음.
//
//  순환 참조 없이 어디서든 포인터/참조 선언에 사용 가능.
//  실제 정의가 필요한 경우 각 개별 헤더를 include 하세요.
// ============================================================

// ------------------------------------------------------------------
// 엔진 기반 타입 (FProperty 등) 전방 선언
// ------------------------------------------------------------------
class FProperty;
class UObject;

// ------------------------------------------------------------------
// 리플렉션 구조체 전방 선언
// ------------------------------------------------------------------
struct FPropertyInfo;
struct FParameterInfo;
struct FFunctionInfo;
struct FClassInfo;
struct FStructInfo;
struct FEnumValueInfo;
struct FEnumInfo;

// ------------------------------------------------------------------
// 열거형 전방 선언
// ------------------------------------------------------------------
enum EPropertyFlags : uint32;
enum class EStructEditorWidget : uint8;
